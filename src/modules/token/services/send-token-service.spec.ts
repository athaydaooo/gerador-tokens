describe('Send token', () => {
  beforeAll(() => {
  })

  it('Should create a token and return it', async () => {
  })

  it('Should return an AppError if token is empty', async () => {
    
  })

  it('Should return an AppError if token exists', async () => {
   
  })

})
